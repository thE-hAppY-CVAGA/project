# Система управления продажами

PyQt6 приложение для управления продажами с системой аутентификации, управлением товарами, клиентами и отчетами.

## Функциональность

- 🔐 **Аутентификация пользователей** - система входа с хэшированием паролей
- 👥 **Управление пользователями** - добавление, редактирование и удаление пользователей
- 📦 **Управление товарами** - каталог продукции с ценами и описаниями
- 👤 **Управление клиентами** - база данных клиентов
- 💰 **Управление продажами** - оформление и отслеживание продаж
- 📊 **Отчеты** - аналитика продаж и финансовых показателей

## Технологии

- **Python 3.8+**
- **PyQt6** - графический интерфейс
- **PostgreSQL** - база данных
- **Docker & Docker Compose** - контейнеризация
- **bcrypt** - хэширование паролей

## Установка и запуск

### Предварительные требования

- Python 3.8+
- Docker и Docker Compose

### 🚀 Быстрый запуск с Docker

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/thE-hAppY-CVAGA/project.git
   cd project
   ```

2. **Запустите базу данных:**
   ```bash
   docker-compose up -d
   ```

3. **Установите зависимости Python:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте конфигурацию:**
   ```bash
   cp config.example.ini config.ini
   # Отредактируйте config.ini при необходимости
   ```

5. **Запустите приложение:**
   ```bash
   python main.py
   ```

### 🔧 Ручная настройка (альтернативный вариант)

Если вы предпочитаете использовать локальную PostgreSQL:

1. **Установите PostgreSQL** на вашу систему

2. **Создайте базу данных:**
   ```sql
   CREATE DATABASE "shoe_sales-db";
   ```

3. **Импортируйте схему:**
   ```bash
   psql -U postgres -d shoe_sales-db -f init-db.sql
   ```

4. **Настройте config.ini** для подключения к вашей локальной БД

### 📊 Данные для входа

- **Логин:** admin
- **Пароль:** admin123

### 🔌 Управление базой данных

Приложение автоматически проверяет доступность базы данных при запуске:

- **Если база данных недоступна** - показывает диалог с предложением запустить Docker контейнер
- **Если Docker не установлен** - сообщает об ошибке
- **После успешного запуска** - приложение продолжает работу нормально

Это обеспечивает, что приложение работает только при доступной базе данных!

### 🐳 Полезные команды Docker

```bash
# Остановить базу данных
docker-compose down

# Перезапустить базу данных
docker-compose restart

# Просмотреть логи базы данных
docker-compose logs postgres

# Подключиться к базе данных
docker exec -it shoe_sales_db psql -U postgres -d shoe_sales-db

# Очистить данные (внимание: удалит все данные!)
docker-compose down -v
```

## Структура проекта

```
project/
├── main.py                 # Точка входа в приложение
├── db.py                   # Модуль работы с базой данных
├── requirements.txt        # Зависимости Python
├── config.ini             # Конфигурация (скопируйте из config.example.ini)
├── config.example.ini     # Пример конфигурации
├── docker-compose.yml     # Конфигурация Docker Compose
├── init-db.sql           # Скрипт инициализации БД
└── ui/                   # Пользовательский интерфейс
    ├── auth/             # Аутентификация
    ├── customers/        # Управление клиентами
    ├── products/         # Управление товарами
    ├── sales/           # Управление продажами
    ├── users/           # Управление пользователями
    └── reports/         # Отчеты
```

## Сборка исполняемого файла

Проект включает готовый исполняемый файл `main.exe` для Windows, созданный с помощью PyInstaller.

Для пересборки:
```bash
pyinstaller --onefile --windowed main.py
```

## Автор

Создано в рамках учебного проекта.

## Лицензия

Этот проект предназначен только для образовательных целей.











